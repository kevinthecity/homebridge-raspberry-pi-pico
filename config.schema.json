{
    "pluginAlias": "HomebridgeRaspberryPiPicoPlugin",
    "pluginType": "platform",
    "singular": true,
    "schema": {
        "type": "object",
        "required": ["name", "message"],
        "properties": {
          "name": {
            "type": "string",
            "description": "The name of the accessory."
          },
          "message": {
            "type": "string",
            "description": "The message to send to the Raspberry Pi Pico API when the accessory is toggled."
          }
        }
    }
}